<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Demo.AppMaui.NotesPage"
             x:Name="Root" 
             Title="Notes Page">
    <VerticalStackLayout Padding="20" Spacing="12">

        <Label Text="Gestor de Notas" FontSize="24" />

        <!-- Entrada y botón de agregar -->
        <HorizontalStackLayout>
            <Entry Placeholder="Escribe una nota..."
                   Text="{Binding NewNote}" WidthRequest="200"/>
            <Button Text="Agregar"
                    Command="{Binding AddNoteCommand}" />
        </HorizontalStackLayout>

        <!-- Última acción -->
        <Label Text="{Binding LastAction}" FontAttributes="Italic"/>

        <!-- Lista de notas -->
        <CollectionView ItemsSource="{Binding Notes}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*,Auto" Padding="6">
                        <Label Grid.Column="0" Text="{Binding Title}" VerticalOptions="Center"/>
                        <Button Grid.Column="1" Text="Eliminar"
                                Command="{Binding Source={x:Reference Root}, Path=BindingContext.RemoveNoteCommand}"
                                CommandParameter="{Binding .}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>